--[[
    ⚡ ULTIMATE STEALTH FLY + NOCLIP SCRIPT
    Entwickelt für maximale Tarnung in Roblox (2026)
    Verwendet: BodyVelocity, Property Spoofing, Remote Blocking, HWID-Simulation
    KEIN 100%iger Schutz – aber das Beste, was technisch möglich ist.
--]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- ============================================================
--  KONFIGURATION
-- ============================================================
local Settings = {
    FlyEnabled = false,
    FlySpeed = 35,          -- Niedrige Geschwindigkeit = weniger auffällig
    NoclipEnabled = false,
    WalkSpeed = 16,
}

-- ============================================================
--  ANTI-DETEKTION LAYER 1: FUNCTION HOOKING
--  (Verhindert das Auslesen manipulierter Werte)
-- ============================================================
local function setupHooks()
    local mt = getrawmetatable and getrawmetatable(game) or getmetatable(game)
    if mt then
        local oldIndex = mt.__index
        mt.__index = function(self, key)
            if key == "WalkSpeed" and self:IsA("Humanoid") then
                return 16 -- Täuscht den Standardwert vor
            end
            return oldIndex(self, key)
        end

        local oldNamecall = mt.__namecall
        mt.__namecall = function(self, ...)
            local args = {...}
            local method = tostring(args[1]) or ""
            if method:lower():find("antihack") or method:lower():find("anticheat") then
                return -- Blockiert verdächtige Remote-Events
            end
            return oldNamecall and oldNamecall(self, ...)
        end
        setmetatable(game, mt)
    end
end

-- ============================================================
--  ANTI-DETEKTION LAYER 2: REMOTE BLOCKING
--  (Blockiert bekannte Anti-Cheat-Remotes)
-- ============================================================
local function blockAntiCheatRemotes()
    local function scanAndBlock(parent)
        for _, child in ipairs(parent:GetChildren()) do
            if child:IsA("RemoteEvent") then
                local name = child.Name:lower()
                if name:find("antihack") or name:find("anticheat") or name:find("report") then
                    local oldFire = child.FireServer
                    child.FireServer = function() return end
                end
            end
            if child:IsA("Folder") or child:IsA("Model") then
                scanAndBlock(child)
            end
        end
    end
    scanAndBlock(game:GetService("ReplicatedStorage"))
end

-- ============================================================
--  FLUGSYSTEM (BodyVelocity mit BodyGyro für Stabilität)
-- ============================================================
local flyConnection = nil
local flyBodyVelocity = nil
local flyBodyGyro = nil

local function startFly()
    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    if flyBodyVelocity then flyBodyVelocity:Destroy() end
    if flyBodyGyro then flyBodyGyro:Destroy() end
    if flyConnection then flyConnection:Disconnect() end

    flyBodyVelocity = Instance.new("BodyVelocity")
    flyBodyVelocity.MaxForce = Vector3.new(40000, 40000, 40000)
    flyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
    flyBodyVelocity.Parent = root

    flyBodyGyro = Instance.new("BodyGyro")
    flyBodyGyro.MaxTorque = Vector3.new(40000, 40000, 40000)
    flyBodyGyro.Parent = root

    flyConnection = RunService.Heartbeat:Connect(function()
        if not Settings.FlyEnabled then return end
        if not root or not root.Parent then
            stopFly()
            return
        end

        local moveDirection = Vector3.new(0, 0, 0)
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            moveDirection = moveDirection + root.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            moveDirection = moveDirection - root.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            moveDirection = moveDirection - root.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            moveDirection = moveDirection + root.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            moveDirection = moveDirection + Vector3.new(0, 1, 0)
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
            moveDirection = moveDirection - Vector3.new(0, 1, 0)
        end

        if moveDirection.Magnitude > 0 then
            moveDirection = moveDirection.Unit * Settings.FlySpeed
        end

        flyBodyVelocity.Velocity = moveDirection
        flyBodyGyro.CFrame = root.CFrame
    end)
end

local function stopFly()
    if flyBodyVelocity then flyBodyVelocity:Destroy(); flyBodyVelocity = nil end
    if flyBodyGyro then flyBodyGyro:Destroy(); flyBodyGyro = nil end
    if flyConnection then flyConnection:Disconnect(); flyConnection = nil end
end

local function toggleFly()
    Settings.FlyEnabled = not Settings.FlyEnabled
    if Settings.FlyEnabled then startFly() else stopFly() end
end

-- ============================================================
--  NOCLIP
-- ============================================================
local function toggleNoclip()
    Settings.NoclipEnabled = not Settings.NoclipEnabled
    local char = LocalPlayer.Character
    if char then
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = not Settings.NoclipEnabled
            end
        end
    end
end

-- ============================================================
--  WALKSPEED
-- ============================================================
local function applyWalkSpeed(speed)
    Settings.WalkSpeed = speed
    local char = LocalPlayer.Character
    if char then
        local humanoid = char:FindFirstChild("Humanoid")
        if humanoid then
            humanoid.WalkSpeed = speed
        end
    end
end

-- ============================================================
--  RESPAWN-HANDLER
-- ============================================================
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    if Settings.FlyEnabled then startFly() end
    if Settings.NoclipEnabled then
        local char = LocalPlayer.Character
        if char then
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end
    applyWalkSpeed(Settings.WalkSpeed)
end)

-- ============================================================
--  INITIALISIERUNG
-- ============================================================
setupHooks()
blockAntiCheatRemotes()
task.wait(0.5)
applyWalkSpeed(16)

print("✅ ULTIMATE STEALTH SCRIPT GELADEN!")
print("📌 F = Fly | N = Noclip | [ / ] = WalkSpeed | - / + = FlySpeed")
