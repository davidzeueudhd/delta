--[[
    EXTREMER DESYNC - HARTE TELEPORTATION
    - Kein Gleiten, keine Sanftheit
    - Direkte Positionsänderung
    - 1000 Studs Versatz
    - WARNUNG: 100% BANN!
]]

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local rootPart = character:WaitForChild("HumanoidRootPart")

print("🔥 EXTREMER DESYNC STARTET...")

-- ==============================
-- RESET 1x
-- ==============================
print("🔄 Reset...")
character.Humanoid.Health = 0
local newChar = player.CharacterAdded:Wait()
character = newChar
rootPart = character:WaitForChild("HumanoidRootPart")
print("✅ Respawned!")

-- ==============================
-- GRÜNER MARKER (bei dir)
-- ==============================
local function createMarker(position, text)
    local marker = Instance.new("Part")
    marker.Size = Vector3.new(5, 1, 5)
    marker.Position = position
    marker.Anchored = true
    marker.CanCollide = false
    marker.BrickColor = BrickColor.new("Bright green")
    marker.Material = Enum.Material.Neon
    marker.Transparency = 0.2
    marker.Parent = Workspace
    
    local billboard = Instance.new("BillboardGui")
    billboard.Size = UDim2.new(0, 200, 0, 40)
    billboard.StudsOffset = Vector3.new(0, 3, 0)
    billboard.Parent = marker
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text or "📍 POSITION"
    label.TextColor3 = Color3.fromRGB(0, 255, 0)
    label.TextScaled = true
    label.Font = Enum.Font.Bold
    label.Parent = billboard
    
    local glow = Instance.new("PointLight")
    glow.Parent = marker
    glow.Color = Color3.fromRGB(0, 255, 0)
    glow.Range = 20
    glow.Brightness = 10
    
    game:GetService("Debris"):AddItem(marker, 30)
    print("🟢 Marker gesetzt an: " .. tostring(position))
    return marker
end

-- ==============================
-- FAKE POSITION (1000 STUDS)
-- ==============================
local fakePos = rootPart.Position + Vector3.new(1000, 50, 1000)

-- ==============================
-- MARKER AN FAKE POSITION
-- ==============================
createMarker(fakePos, "🎯 ANDERE SEHEN DICH HIER (1000 STUDS)")
print("🟢 Marker an Fake Position: " .. tostring(fakePos))

-- ==============================
-- EXTREMER DESYNC (HART)
-- ==============================
RunService.Heartbeat:Connect(function()
    if not rootPart or not rootPart.Parent then return end
    
    -- DIREKTE Positionsänderung – KEIN Gleiten!
    local offset = Vector3.new(
        math.random(-100000, 100000) / 100,  -- 1000 Studs
        math.random(-5000, 5000) / 100,
        math.random(-100000, 100000) / 100
    )
    
    -- Harte Teleportation
    rootPart.CFrame = rootPart.CFrame + offset
end)

print("💀 EXTREMER DESYNC LÄUFT!")
print("📌 Andere sehen dich 1000 Studs entfernt!")
print("📌 KEIN Gleiten – HARTE Teleportation!")
print("⚠️ 100% BANN - NUR AUF WEGWERF-ACCOUNTS!")
